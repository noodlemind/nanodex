# QLoRA Training Configuration (4-bit quantization, 12-24GB VRAM)

# Model configuration
base_model: "Qwen/Qwen2.5-Coder-7B"  # Apache-2.0 license
model_max_length: 2048

# Dataset configuration
dataset_path: "data/dataset/train.jsonl"
validation_split: 0.1  # 10% for validation

# QLoRA parameters
quantization:
  load_in_4bit: true
  bnb_4bit_compute_dtype: "bfloat16"
  bnb_4bit_quant_type: "nf4"
  bnb_4bit_use_double_quant: true

# LoRA configuration
lora:
  r: 16  # Rank
  lora_alpha: 32  # Scaling factor
  lora_dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
  bias: "none"
  task_type: "CAUSAL_LM"

# Training hyperparameters
training:
  output_dir: "models/nanodex-qlora"
  num_train_epochs: 3
  per_device_train_batch_size: 4
  per_device_eval_batch_size: 4
  gradient_accumulation_steps: 4
  learning_rate: 2.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.03
  lr_scheduler_type: "cosine"
  logging_steps: 10
  save_steps: 100
  eval_steps: 100
  save_total_limit: 3
  fp16: false
  bf16: true
  max_grad_norm: 0.3
  optim: "paged_adamw_32bit"
  group_by_length: true
  report_to: "tensorboard"

# Generation parameters (for evaluation)
generation:
  max_new_tokens: 512
  temperature: 0.7
  top_p: 0.9
  do_sample: true

# Instruction format
instruction_template: |
  <|im_start|>system
  You are a helpful code assistant specialized in this codebase.<|im_end|>
  <|im_start|>user
  {prompt}<|im_end|>
  <|im_start|>assistant
  {response}<|im_end|>
